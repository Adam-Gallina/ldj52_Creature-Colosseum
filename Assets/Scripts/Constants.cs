using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public static class Constants
{
    public const int CardZoneLayer = 6;

    /*#region Crops
    public static CropClass GetCropClass(CropType crop)
    {
        for (int i = 0; i < cropTypes.Length; i++)
        {
            if (cropTypes[i] == crop)
                return cropClasses[i];
        }

        Debug.LogError("Could not find CropClass for " + crop);
        return CropClass.None;
    }
    private static CropType[] cropTypes = new CropType[]
    {
        CropType.None,
        CropType.Carrot,
    };
    private static CropClass[] cropClasses = new CropClass[]
    {
        CropClass.None,
        CropClass.Vegetable,
    };
    #endregion*/
}
